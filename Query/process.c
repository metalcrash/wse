#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* process(int termnum, char* strindexls, char* strappears){
	char* indexls[termnum]
	char* appears[termnum]
	splitInput(indexls,appears,strindexls,strappears)
}

void splitInput(char* indexls[],char* appears[],char* strindexls,char* strappears){

}